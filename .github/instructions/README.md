# Scoped Copilot Instructions

Place repository-scoped instruction files here when a directory or file pattern needs rules beyond the repository baseline.

Current scoped instruction files:

- `components.instructions.md`: rules for Dashticz component source in `js/components/`

Keep each file short, specific, and limited to rules that are repeatedly useful during implementation or review.