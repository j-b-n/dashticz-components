# Runbooks

Use this directory for repeatable operational procedures.

Current runbooks:

- `deployment.md`: install or update the component set in a Dashticz instance.

Add new runbooks when maintenance work requires multi-step instructions that should not live in repository instructions.